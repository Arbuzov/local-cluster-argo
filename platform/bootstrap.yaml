apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/Arbuzov/home-k8s-argo.git
    targetRevision: main
    path: platform
    directory:
      recurse: true
      include: '{project.yaml,*/application*.yaml}'
      exclude: '{kubernetes-dashboard/application.yaml,metrics-server/application.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-cd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
